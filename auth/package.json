{
  "name": "auth",
  "version": "1.0.0",
  "scripts": {
    "compile": "cp ../tsconfig.json.template ./tsconfig.json && tsc"
  },
  "license": "Apache 2.0",
  "dependencies": {
    "firebase": "^9.2.0",
    "firebaseui": "^6.0.0"
  }
}
